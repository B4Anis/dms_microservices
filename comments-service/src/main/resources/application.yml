server:
  port: 8081
spring:
  data:
    cassandra:
      contact-points: cassandra
      port: 9042
      keyspace-name: dms
      local-datacenter: datacenter1
      schema-action: create-if-not-exists

